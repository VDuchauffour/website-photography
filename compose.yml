services:
  dev:
    image: ghcr.io/gohugoio/hugo:v0.157.0
    command: server --bind 0.0.0.0 -D --poll 500ms
    volumes:
      - .:/project
    ports:
      - "1313:1313"
    profiles: [dev]

  prod:
    build: .
    ports:
      - "8080:80"
    profiles: [prod]
